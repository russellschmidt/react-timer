var React = require("react");

var WeatherTitle = React.createClass({
  render: function () {
    return (
      <div>
        <h1>Get Weather</h1>
      </div>
    );
  }
});

module.exports = WeatherTitle;
